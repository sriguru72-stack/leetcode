class Solution {
    public String largestOddNumber(String num) {
        String value;
        for(int i=num.length()-1;i>=0;i--){
            if(num.charAt(i)%2!=0){
                value=num.substring(0,i+1);
                return value;
            }
        }
        return "";
    }
}
